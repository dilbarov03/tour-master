from django.apps import AppConfig


class TourCatalogConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.tour_catalog'
    verbose_name = 'Tayyor tur paketlar'
